export type MainInfo = {
  url: string;
  name: string;
  short: string;
  country: string;
};

export type TitleArgs = {
  eventTitle: string;
  theater: string;
  city: string;
  appName: string;
};
